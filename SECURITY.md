# Security

Do not commit Discord tokens, GitHub tokens, Brave API keys, Palworld passwords,
server addresses, player data, save files, or generated vector databases.

Use `.env` only on the machine running the companion. The tracked
`.env.example` contains placeholders. Public bug reports must redact server
identifiers, player identifiers, and credentials.

Report vulnerabilities privately through GitHub's security advisory interface.
